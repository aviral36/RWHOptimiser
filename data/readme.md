### This section contains all the data which is being used by our model. Most of the files are in CSV. 

NOTE: In case a file import does not work, it is possibly because of incorrect path fed into the code. Recheck the path and run again.
